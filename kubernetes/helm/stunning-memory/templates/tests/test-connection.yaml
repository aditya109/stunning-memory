apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "stunning-memory.fullname" . }}-test-connection"
  labels:
    {{- include "stunning-memory.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "stunning-memory.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
